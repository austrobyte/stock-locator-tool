import React from "react";
import { IncomingColors } from "../types";
type FooterProps = {
    theme: IncomingColors;
};
declare const Footer: ({ theme }: FooterProps) => React.JSX.Element;
export default Footer;
