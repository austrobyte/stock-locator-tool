import React from "react";
import { IncomingColors } from "../types";
type SponsoredProps = {
    theme: IncomingColors;
};
declare const Sponsored: ({ theme }: SponsoredProps) => React.JSX.Element;
export default Sponsored;
